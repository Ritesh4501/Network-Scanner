# Network-Scanner
A basic network scanner built using Python that scans a range of IP addresses to detect live hosts using ICMP ping and optionally checks for open TCP ports (like port 80). It uses multithreading for efficient scanning and is cross-platform, making it ideal for learning network discovery basics.
